import { showExample, showOutput } from "./shared/utils.js";

// LOOPING OVER AN ARRAY

// Task
// create a new array
// from an existing array
// call a function on every element

showExample(1);

let ex1_original = [1, 2, 3, 4, 5];
let ex1_new = [1];

showOutput(ex1_original, ex1_new, 1);
